---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import CTA from '../components/CTA.astro';
import ProfileSelector from '../components/ProfileSelector.astro';

import { contentfulClient } from '../lib/contentful';
import { SITE_CONFIG } from '../lib/config.ts';
import type { Hero as HeroType } from '../lib/types.ts';

const heroRes = await contentfulClient.getEntries({ content_type: 'hero' });
const hero: HeroType = heroRes.items[0]?.fields || {
  title: 'Coral | Pentester',
  subtitle: 'Ethical hacking Málaga',
  ctaLabel: 'Ver Proyectos',
  ctaUrl: '/proyectos'
};
---

<Layout title={SITE_CONFIG.title}>
  <ProfileSelector />
</Layout>